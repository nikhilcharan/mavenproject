pipeline {
    agent any 
    stages {
        stage('Git Checkout') { 
            steps {
                git credentialsId: 'nikhilcharan', url: 'https://github.com/nikhilcharan/devopstraining.git'
            }
        }  
        
        stage('Build') {
             steps {
                   mvn clean install
           }
        }
    }
  }
