pipeline {
    agent any 
    tools {
        maven '3.6.3'
        jdk 'JAVA_HOME'
    }
    stages {
        stage('Checkout') { 
            steps {
                git credentialsId: 'nikhilcharan', url: 'https://github.com/nikhilcharan/devopstraining.git'
            }
        }  
        
        stage('Build') {
             steps {
                   mvn clean install
           }
        }
    }
  }
