pipeline {
    agent any 
    tools {
        maven 'mavenHome'
        jdk 'JavaHome'
    }
    stages {
        stage('Checkout') { 
            steps {
                git credentialsId: 'nikhilcharan', url: 'https://github.com/nikhilcharan/devopstraining.git'
            }
        }  
        
        stage('Build') {
             steps {
                   mvn clean install
           }
        }
    }
  }
